dist
coverage
node_modules
playwright-report
test-results
stats.html
package-lock.json

# The architecture package is authored prose with hand-aligned tables and
# Mermaid diagrams. Reformatting it produces large diffs that hide the real
# documentation changes.
docs
IMPLEMENTATION_STATUS.md

# The JSONTestSuite corpus. Many of these files are deliberately malformed —
# invalid UTF-8, lone surrogates, truncated escapes — and formatting them
# would silently weaken the conformance suite.
tests/fixtures/jsontestsuite/
# The manifest is generated; reformatting it would change every hash line's
# position for no benefit.
tests/fixtures/jsontestsuite.checksums.json
